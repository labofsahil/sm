// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'package:freezed_annotation/freezed_annotation.dart' hide protected;
part 'sendme.freezed.dart';

// These functions are ignored because they are not marked as `pub`: `canonicalized_path_to_string`, `export_with_progress`, `get_export_path`, `get_runtime`, `import_with_progress`, `start_receive_inner`, `start_send_inner`, `validate_path_component`
// These types are ignored because they are neither used by any `pub` functions nor (for structs and enums) marked `#[frb(unignore)]`: `ReceiveSession`, `SendSession`
// These function are ignored because they are on traits that is not defined in current crate (put an empty `#[frb]` on it to unignore): `clone`, `clone`, `fmt`, `fmt`

Stream<SendProgress> startSend({
  required String path,
  required String tempDir,
}) =>
    RustLib.instance.api.crateApiSendmeStartSend(path: path, tempDir: tempDir);

Future<void> stopSend() => RustLib.instance.api.crateApiSendmeStopSend();

Stream<ReceiveProgress> startReceive({
  required String ticketStr,
  required String tempDir,
  required String destinationDir,
}) => RustLib.instance.api.crateApiSendmeStartReceive(
  ticketStr: ticketStr,
  tempDir: tempDir,
  destinationDir: destinationDir,
);

Future<void> cancelReceive() =>
    RustLib.instance.api.crateApiSendmeCancelReceive();

@freezed
sealed class ReceiveProgress with _$ReceiveProgress {
  const ReceiveProgress._();

  const factory ReceiveProgress.connecting() = ReceiveProgress_Connecting;
  const factory ReceiveProgress.connected() = ReceiveProgress_Connected;
  const factory ReceiveProgress.retrievingMetadata() =
      ReceiveProgress_RetrievingMetadata;
  const factory ReceiveProgress.downloading({
    required BigInt bytesDownloaded,
    required BigInt totalBytes,
    required double percentage,
  }) = ReceiveProgress_Downloading;
  const factory ReceiveProgress.downloadDone({required BigInt totalBytes}) =
      ReceiveProgress_DownloadDone;
  const factory ReceiveProgress.exporting({
    required String fileName,
    required BigInt bytesExported,
    required BigInt bytesTotal,
  }) = ReceiveProgress_Exporting;
  const factory ReceiveProgress.finished({
    required BigInt totalFiles,
    required BigInt totalBytes,
  }) = ReceiveProgress_Finished;
  const factory ReceiveProgress.failed({required String error}) =
      ReceiveProgress_Failed;
}

@freezed
sealed class SendProgress with _$SendProgress {
  const SendProgress._();

  const factory SendProgress.importing({
    required String fileName,
    required BigInt bytesDone,
    required BigInt bytesTotal,
  }) = SendProgress_Importing;
  const factory SendProgress.importDone({required BigInt totalSize}) =
      SendProgress_ImportDone;
  const factory SendProgress.startingEndpoint() = SendProgress_StartingEndpoint;
  const factory SendProgress.sharing({required String ticket}) =
      SendProgress_Sharing;
  const factory SendProgress.failed({required String error}) =
      SendProgress_Failed;
}
